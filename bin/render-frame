#!perl
use strict;
use warnings;
use Getopt::Long qw( GetOptions );
use autodie qw( system );

GetOptions(
  help    => sub { die },
  'dir=s' => \ my $dir,
  'in=s'  => \ my $in,
  'png=s' => \ my $png
)
  or die "@ARGV";

system 'frame-read',       '--dir', $dir, '--in', $in;
system 'frame-make-graph', '--dir', $dir;
system 'frame-deparent',   '--dir', $dir;
system 'frame-redge',      '--dir', $dir;
system 'frame-sum-size',   '--dir', $dir;
system 'frame-names',      '--dir', $dir;
system 'frame-png',        '--dir', $dir, '--png', $png;
